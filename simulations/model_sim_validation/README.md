To run, set the number of threads in the Makefile and then use:
```
make run-all
```

To get summaries and plot (need to have pandas, matplotlib, and seaborn environment active):
```
make collect-results
```